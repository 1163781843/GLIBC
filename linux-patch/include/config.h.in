#ifndef __CONFIG_H__
#define __CONFIG_H__

/*! CC Header Files */
#cmakedefine HAVE_STDIO_H
#ifdef HAVE_STDIO_H
#include <stdio.h>
#endif

#cmakedefine HAVE_STRING_H
#ifdef HAVE_STRING_H
#include <string.h>
#endif

#cmakedefine HAVE_STDARG_H
#ifdef HAVE_STDARG_H
#include <stdarg.h>
#endif

#cmakedefine HAVE_STDLIB_H
#ifdef HAVE_STDLIB_H
#include <stdlib.h>
#endif

#cmakedefine HAVE_UNISTD_H
#ifdef HAVE_UNISTD_H
#include <unistd.h>
#endif

#cmakedefine HAVE_TIME_H
#ifdef HAVE_TIME_H
#include <time.h>
#endif

#cmakedefine HAVE_DLFCN_H
#ifdef HAVE_DLFCN_H
#include <dlfcn.h>
#endif

#cmakedefine HAVE_SYS_PTRACE_H
#ifdef HAVE_SYS_PTRACE_H
#include <sys/ptrace.h>
#endif

#cmakedefine HAVE_SYS_TIME_H
#ifdef HAVE_SYS_TIME_H
#include <sys/time.h>
#endif

#cmakedefine HAVE_SYS_SYSCALL_H
#ifdef HAVE_SYS_SYSCALL_H
#include <sys/syscall.h>
#endif

#cmakedefine HAVE_SYS_TYPES_H
#ifdef HAVE_SYS_TYPES_H
#include <sys/types.h>
#endif

#cmakedefine HAVE_SYS_USER_H
#ifdef HAVE_SYS_USER_H
#include <sys/user.h>
#endif

#cmakedefine HAVE_SYS_WAIT_H
#ifdef HAVE_SYS_WAIT_H
#include <sys/wait.h>
#endif

/*! CXX Header Files */
#cmakedefine HAVE_IOSTREAM_H
#ifdef HAVE_IOSTREAM_H
#include <iostream>
#endif

#cmakedefine HAVE_LIST_H
#ifdef HAVE_LIST_H
#include <list>
#endif

#cmakedefine HAVE_MEMORY_H
#ifdef HAVE_MEMORY_H
#include <memory>
#endif

#cmakedefine HAVE_FSTREAM_H
#ifdef HAVE_FSTREAM_H
#include <fstream>
#endif

typedef @CMAKEDEFINE_INT32_T@ int32b_t;

typedef @CMAKEDEFINE_UINT32_T@ uint32b_t;

typedef @CMAKEDEFINE_LONG_T@ long_t;

typedef @CMAKEDEFINE_ULONG_T@ ulong_t;

typedef @CMAKEDEFINE_INT8_T@ int8b_t;

typedef @CMAKEDEFINE_UINT8_T@ uint8b_t;

typedef @CMAKEDEFINE_INT16_T@ int16b_t;

typedef @CMAKEDEFINE_UINT16_T@ uint16b_t;

typedef @CMAKEDEFINE_VOID_T@ void_t;

#define uint32_mask_t @CMAKEDEFINE_UINT32_MASK@

#define uint8_mask_t @CMAKEDEFINE_UINT8_MASK@

#define uint16_mask_t @CMAKEDEFINE_UINT16_MASK@

#define ulong_mask_t @CMAKEDEFINE_ULONG_MASK@

#define LD @CMAKEDEFINE_LD_T@
#define LU @CMAKEDEFINE_LU_T@
#define LX @CMAKEDEFINE_LX_T@

#endif